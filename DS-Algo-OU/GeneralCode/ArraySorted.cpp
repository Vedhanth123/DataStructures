#include <iostream>
using namespace std;

int start = 0;

// function to check if an array is sorted or not
bool is_sorted(int *array, int n)
{
    if()
}

int main()
{
    int array[] = {1, 2, 3, 9, 10, 0, -1};
    cout << is_sorted(array, 0, 0, sizeof(array) / sizeof(int), array[0]);
    return 1;
}